DssVue-Example-Plugins


HelloPlugin  -- super simple plugin that adds a "Hello Plugin" item to the Tools Menu.  

ToTextPlugin -- takes selected time series data sets and writes the data out to a text file.